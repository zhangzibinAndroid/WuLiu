<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <!--侧滑滑动返回上一页  可以不用，如果想自定义动画可以用来设置-->
        <!-- <item name="android:windowAnimationStyle">@style/Animation_Right</item>
 -->

    </style>


    <style name="TextViewStyle">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:padding">@dimen/px40</item>
        <item name="android:textSize">@dimen/px25</item>
    </style>


    <style name="ViewStyle">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">@dimen/px02</item>
        <item name="android:background">#E9EDEE</item>
        <item name="android:layout_marginLeft">@dimen/px40</item>
        <item name="android:layout_marginRight">@dimen/px40</item>
    </style>
    <!--  =================     下面   二维码扫一扫         ===========-->

    <!-- 适用于开启滑动返回功能的Activity -->
    <style name="AppTheme.Transparent">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
    </style>

    <!-- 适用于欢迎界面避免第一次进来白屏或黑屏 -->
    <style name="AppTheme.Transparent.Splash">
        <item name="android:windowDisablePreview">true</item>
    </style>

    <style name="MatchWrapTv">
        <item name="android:textSize">12sp</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:gravity">center</item>
        <item name="android:background">@drawable/selector_btn_orange</item>
        <item name="android:layout_margin">10dp</item>
        <item name="android:onClick">onClick</item>
        <item name="android:padding">8dp</item>
    </style>


    <style name="MatchMatch">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
    </style>
    <!--  =================    上面    二维码扫一扫         ===========-->

    <!--=================================== toolbar和状态栏颜色 ====================================================-->

    <!--  和styles 19 版本中一起用 侧滑滑动返回上一页 设置背景透明，
    右滑时才能看到上一个界面，否则会看到黑屏效果 和 右滑时  状态栏和页面一起带动滑动 -->
    <style name="AppTheme.ColorStatusSwipe">
        <item name="android:windowIsTranslucent">true</item>
    </style>
    <!--popWindow弹出动画-->
    <style name="dialogWindowAnim_style">
        <item name="android:windowEnterAnimation">@anim/dialog_enter_anim_top_bottom</item>
        <item name="android:windowExitAnimation">@anim/dialog_exit_anim_top_bottom</item>
    </style>





</resources>
